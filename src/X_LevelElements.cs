namespace YGR
{
    /// <summary>
    /// Enum X_LevelElements enumerates the different types of elements that can be inside a level.
    /// This can be a room, connector, victim, projectile etc.
    /// </summary>
    public enum X_LevelElements
    {
        Room = 0,
        Door,
        Victim,
        Enemy,
        Invincible,
        Ghost,
        Projectile,
        Level,
        Interactable,
    }
}
